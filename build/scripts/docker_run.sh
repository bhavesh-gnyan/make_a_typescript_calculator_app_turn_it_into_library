# Run the Calculator Library with Docker

To run your calculator library as a container, follow these steps:

1. Build the image using the provided `Dockerfile` and ensure it's in the same directory as this script.